server:
  port: 9002

spring:
  application:
    name: cloud-provider-payment

eureka:
  client:
    service-url:
      defaultZone: http://eureka1.com:7001/eureka/,http://eureka2.com:7002/eureka/
  instance:
    prefer-ip-address: true # 使用ip地址展示
    instance-id: cloud-club.wetech.cloud.provider.provider-payment-9002 # 实例名称
